import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const Home = () => {
  return (
     <SafeAreaView>
        <Text className='px-6'>Home!</Text>
    </SafeAreaView>
  )
}

export default Home